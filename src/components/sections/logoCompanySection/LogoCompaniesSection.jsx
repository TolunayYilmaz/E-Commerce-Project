import LogoItems from "../../CardList/logoCompanyList/LogoItems.jsx";

export default function LogoCompaniesSection({logos}){

    return(<section>
        <LogoItems logos={logos} />
    </section>)
}